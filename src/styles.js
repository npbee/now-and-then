import { colors } from './colors';

export const typography = {
    color: colors.black,
    fontFamily: '-apple-system, BlinkMacSystemFont, Avenir, Arial, sans-serif'
};

export const scale = [
    0,
    8,
    16,
    32,
    64,
];

export const styles = {
    typography,
    scale,
};
